Your exported survey spreadsheets are saved here by default.

This is the default output folder for KBC Flyer Reader. Each extraction
run creates a new .xlsx file here (or appends to an existing one if you
chose that option).

You can change where files are saved in Settings → "Default output folder"
→ Browse. You can safely delete this text file.
